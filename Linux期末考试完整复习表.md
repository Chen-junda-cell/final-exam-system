# Linux 期末考试完整复习表

> 综合自：课件（第01~12讲）、8套练习题（78道选择题 + 简答题）、原复习重点笔记

---

## 一、Linux 基础概述

| 考点 | 内容 |
|------|------|
| 定义 | 类 UNIX 操作系统，内核由 **Linus Torvalds** 开发 |
| 特点 | 与 UNIX 兼容、自由软件/源码公开、高性能高安全、便于定制和再开发、互操作性高、多用户多任务、32/64位 |
| 自由软件含义 | 软件可**自由修改和发布**（非免费之意） |
| 主流发行版 | **CentOS 7**（考试用）、RedHat、SUSE、Ubuntu |
| 非 Linux 系统 | Windows XP 等 |
| 应用领域 | 服务器市场、嵌入式系统 |
| 默认 Shell | **bash**（Bourne Again Shell） |
| root 提示符 | **`#`**；普通用户提示符：**`$`** |
| 安装方式 | 图形安装（资源消耗多）、字符安装（资源消耗少）；若图形无法启动会自动转字符 |

---

## 二、开关机 / 重启命令

| 操作 | 命令 | 常用选项 |
|------|------|----------|
| 关机 | `shutdown`、`poweroff`、`halt` | `shutdown -h now` 立即关机；`shutdown -h +10` 10分钟后关机 |
| 重启 | `reboot` | `shutdown -r now` 立即重启 |

---

## 三、目录结构与路径

### 3.1 核心目录及作用（高频 ★★★）

| 目录 | 作用 |
|------|------|
| `/` | 根目录，文件系统起点，仅 root 可写 |
| `/bin` | 存放系统**基本命令**（ls、cp、mv 等），所有用户可用 |
| `/sbin` | 系统**管理员**专用命令（fdisk、ifconfig），普通用户通常无执行权限 |
| `/boot` | 系统**启动**所需文件（内核镜像、GRUB 引导程序） |
| `/dev` | **设备文件**目录，硬件抽象为文件（/dev/sda、/dev/tty） |
| `/etc` | 系统和服务的**配置文件**目录（/etc/passwd、/etc/network） |
| `/home` | 普通用户**主目录**，每个用户有私有目录 |
| `/lib` | 系统核心**库文件**，为 /bin 和 /sbin 提供依赖 |
| `/media` | 可移动设备**自动挂载点**（U盘、光盘） |
| `/mnt` | 手动挂载外部设备的**临时挂载点** |
| `/opt` | 第三方软件默认安装目录 |
| `/proc` | **虚拟文件系统**，实时反映内核和进程状态，不占磁盘 |
| `/root` | 超级用户 root 的主目录 |
| `/tmp` | 临时文件目录，所有用户可读写，**重启后清空** |
| `/usr` | 用户级应用程序和资源（/usr/bin、/usr/lib、/usr/share） |
| `/var` | 动态变化数据（日志 /var/log、缓存 /var/cache、邮件队列） |

### 3.2 路径操作

| 操作 | 命令 |
|------|------|
| 返回上级 | `cd ..` |
| 进入根目录 | `cd /` |
| 回用户主目录 | `cd`（无参数）或 `cd ~` |
| 切换到 John 的主目录 | `cd ~John` |
| 查看当前绝对路径 | `pwd` |
| 注意 | `cd` 后**必须加空格** |

---

## 四、文件与目录操作命令

### 4.1 基础操作

| 操作 | 命令 | 说明 |
|------|------|------|
| 新建空文件 | `touch 文件名` | |
| 删除文件 | `rm 文件名` | |
| 复制文件 | `cp 源文件 目标文件` | |
| 移动/重命名 | `mv 源文件 目标文件` | mv 兼具移动和重命名功能 |
| 创建目录 | `mkdir 目录名` | `-p` 递归创建父目录 |
| 查看文件类型 | `file 文件名` | |

### 4.2 查看文件内容（高频 ★★★）

| 命令 | 特点 | 适用场景 |
|------|------|----------|
| `cat` | 一次性输出全部内容 | 小文件；可合并多个文件、追加内容 |
| `more` | 分页显示，只支持**向下翻页**，启动时加载整个文件 | 基础分页 |
| `less` | 分页显示，支持**上下翻页、搜索、跳转**，退出不残留 | 大文件查看（最灵活） |
| `head` | 默认显示文件**前10行**；`head -n 20` 前20行 | 快速查看文件开头 |
| `tail` | 默认显示文件**最后10行**；`tail -n 20` 后20行 | 查看文件末尾；`tail -f` 实时追踪日志 |

### 4.3 文件列表 ls

| 命令 | 作用 |
|------|------|
| `ls` | 列出非隐藏文件 |
| `ls -a` | 列出**所有文件**（含隐藏文件） |
| `ls -l` | 显示文件**详细信息**（权限、所有者、大小、时间） |
| `ls -a?` | 列出以 a 开头、仅两个字符的文件（`?` 匹配单个字符） |

### 4.4 重定向

| 符号 | 作用 |
|------|------|
| `>` | 标准输出重定向，**覆盖**目标文件 |
| `>>` | 标准输出重定向，**追加**到目标文件 |
| `2>&1` | 将**标准错误输出重定向到标准输出** |

---

## 五、用户与组管理

### 5.1 基本命令

| 操作 | 命令 | 说明 |
|------|------|------|
| 添加用户 | `useradd 用户名` | |
| 设置/修改密码 | `passwd 用户名` | |
| 删除用户（仅删账号） | `userdel 用户名` | 家目录**保留** |
| 删除用户+家目录 | `userdel -r 用户名` | **重点：账号+家目录全删** |
| 修改用户账户 | `usermod` | 可重命名、加锁/解锁，**不能删除账户** |
| 切换用户 | `su 用户名` | `su` 切换为 root |
| 用户信息文件 | `/etc/passwd` | 存放用户账号基本信息 |

### 5.2 userdel -r 详解

| 方式 | 结果 |
|------|------|
| 不加 `-r` | 仅删除账号，`/home/用户名/` 目录**保留** |
| 加 `-r` | 账号 + 家目录内全部文件**一起删除** |

---

## 六、文件权限管理（高频 ★★★）

### 6.1 权限表示

| 权限 | 符号 | 数字值 |
|------|:---:|:------:|
| 读 | `r` | 4 |
| 写 | `w` | 2 |
| 执行 | `x` | 1 |

### 6.2 数字权限速算

| 数字 | 权限组合 | 含义 |
|:----:|----------|------|
| 7 | rwx | 读+写+执行 |
| 6 | rw- | 读+写 |
| 5 | r-x | 读+执行 |
| 4 | r-- | 只读 |
| 0 | --- | 无权限 |

### 6.3 chmod 修改权限

**数字形式（推荐）：** `chmod 数字 文件/目录`

三位数字分别代表：**所有者 | 所属组 | 其他人**

| 示例 | 结果 |
|------|------|
| `chmod 764 file` | 所有者 rwx，组 rw-，其他人 r-- |
| `chmod 755 file` | 所有者 rwx，组 r-x，其他人 r-x |
| `chmod 777 file` | **全部放满** rwxrwxrwx（风险极高，仅用于测试） |

**字符形式：**

| 符号 | 含义 | 示例 |
|------|------|------|
| `u` | 所有者 | `chmod u+x file`（所有者加执行） |
| `g` | 所属组 | `chmod g-w file`（组去写） |
| `o` | 其他人 | `chmod o+r file`（其他人加读） |
| `a` | 所有人 | `chmod a+rwx file` = 777 |

### 6.4 chown vs chmod（易混淆）

| 命令 | 作用 |
|------|------|
| `chmod` | 修改文件/目录的**读写执行权限** |
| `chown` | 修改文件/目录的**所属用户和组** |

---

## 七、链接（软链接 / 硬链接 ★★★）

| 概念 | 软链接（符号链接） | 硬链接 |
|------|-------------------|--------|
| 本质 | 独立文件，存储**原文件路径** | 与原文件**共用 inode**，文件别名 |
| 类比 | Windows 快捷方式 | 文件的另一个名字 |
| 跨文件系统 | ✅ 支持 | ❌ 不支持 |
| 跨目录 | ✅ 支持 | ❌ 不支持 |
| 删除原文件后 | 链接**失效** | 链接**仍有效** |
| 创建命令 | `ln -s 源文件 目标` | `ln 源文件 目标` |

---

## 八、打包与压缩

### 8.1 tar 命令（高频 ★★★）

| 操作 | 命令 | 选项含义 |
|------|------|----------|
| 打包并用 gzip 压缩 | `tar -zcvf 包名.tar.gz 源` | z=gzip, c=创建, v=详细, f=指定文件 |
| 解压 tar.gz | `tar -xzvf 包名.tar.gz` | x=解压 |
| 解压到指定目录 | `tar -xzvf 包名.tar.gz -C /目标目录` | -C 指定目录 |
| 查看 tar 包内容（详细） | `tar -tvf 包名.tar` | t=列出内容 |
| 仅打包不压缩 | `tar -cvf 包名.tar 源` | 无 z |
| 解压 zip | `unzip 文件名.zip` | |

### 8.2 常见组合速记

```
tar -zcvf    → 压缩 （z=zip, c=create）
tar -xzvf    → 解压 （x=extract）
tar -tvf     → 查看 （t=list）
```

---

## 九、vim / vi 文本编辑器（核心考点 ★★★）

### 9.1 三大模式

| 模式 | 进入方式 | 功能 | 能输入文字？ |
|------|----------|------|:--:|
| **命令模式** | 打开 vim 默认进入 | 光标移动、复制、删除、粘贴、撤销 | ✗ |
| **输入模式** | 按 `i` / `a` / `o` | 正常编写修改文本 | ✓ |
| **末行模式** | 命令模式按 `:` | 保存、退出、查找替换、设置参数 | ✗ |

### 9.2 进入输入模式的三种方式

| 按键 | 效果 |
|------|------|
| `i` | 在光标**当前位置**插入（最常用） |
| `a` | 在光标**后一位**插入 |
| `o` | 在光标**下一行**新建空行并进入编辑 |

### 9.3 末行模式常用命令

| 命令 | 作用 |
|------|------|
| `:w` | 保存 |
| `:q` | 退出（未修改才能退） |
| `:wq` 或 `:x` | **保存并退出**（万能） |
| `:q!` | **强制退出不保存** |
| `:set nu` | 显示行号 |
| `:set nonu` | 取消行号 |
| `:n1,n2 co n3` | 将 n1~n2 行**复制**到 n3 行下 |

### 9.4 命令模式常用操作

| 操作 | 按键 | 说明 |
|------|------|------|
| 光标移动 | `h`左 `j`下 `k`上 `l`右 | 四方向键替代 |
| 删除整行 | `dd` | **高频考点** |
| 删除单字符 | `x` | |
| 复制整行 | `yy` | |
| 粘贴 | `p` | 粘贴到下一行 |
| 撤销 | `u` | |
| 重复上次操作 | `.` | |

### 9.5 模式切换图

```
命令模式 ←—→ 输入模式    (i/a/o 进入，Esc 返回)
命令模式 ——→ 末行模式    (按 : 进入)
末行模式 ——→ 命令模式    (执行完自动返回 / 按 Esc)
```

### 9.6 完整实操流程

1. `vim 文件名` → 进入命令模式
2. 按 `i` → 进入输入模式，编写内容
3. 按 `Esc` → 回到命令模式
4. 按 `:` → 进入末行模式，输入 `:wq` → 保存退出

---

## 十、Shell 编程（高频 ★★★）

### 10.1 Shell 基础

| 考点 | 内容 |
|------|------|
| Shell 定义 | 用户与 Linux 内核之间的**命令解释器**，不是编译型语言 |
| 脚本第一行 | **必须** `#!/bin/bash` |
| 注释符 | `#` |
| 默认 Shell | **bash** |
| 执行脚本 | `chmod +x 脚本.sh` → `./脚本.sh` |

### 10.2 变量

| 操作 | 语法 | 说明 |
|------|------|------|
| 声明变量 | `变量名=值` | **等号两侧不能有空格** |
| 使用变量 | `$变量名` 或 `${变量名}` | |
| 声明整数变量 | `declare -i 变量` | 自动计算算术值 |
| 声明数组变量 | `declare -a 变量` | |
| export | 将变量设为**环境变量**，子 Shell 可用 | |

### 10.3 引号

| 引号 | 行为 |
|------|------|
| 双引号 `" "` | **解释**内部变量和特殊字符 |
| 单引号 `' '` | **不解释**任何特殊字符，原样输出 |

### 10.4 数值比较

| 运算符 | 含义 |
|--------|------|
| `-eq` | 等于 (equal) |
| `-ne` | 不等于 (not equal) |
| `-gt` | 大于 (greater than) |
| `-lt` | 小于 (less than) |
| `-ge` | 大于等于 |
| `-le` | 小于等于 |

### 10.5 条件与循环

```bash
# if 语句
if [ 条件 ]; then
    命令
elif [ 条件 ]; then
    命令
else
    命令
fi

# for 循环
for ((i=1; i<=10; i++)); do
    命令
done

# while 循环
while [ 条件 ]; do
    命令
done

# case 语句
case $变量 in
    1) 命令 ;;
    2) 命令 ;;
    *) 命令 ;;   # 默认
esac
```

### 10.6 位置参数与输入

| 语法 | 含义 |
|------|------|
| `$1`, `$2`, ... | 第N个位置参数 |
| `$#` | 参数个数 |
| `$0` | 脚本名 |
| `read -p "提示" 变量` | 读取用户输入 |
| `$((算术式))` | 算术运算 |
| `$RANDOM` | 生成随机数 |

### 10.7 常见简答模板

**① 判断文件是否存在并处理：**
```bash
#!/bin/bash
if [ -e /root/test ]; then          # -e 判断存在
    if [ -f /root/test ]; then      # -f 判断普通文件
        cp /root/test ./
        echo "完成"
    fi
else
    echo "文件不存在"
fi
```

**② 判断是否为字符设备文件：** `[ -c 文件名 ]`

**③ 计算阶乘：**
```bash
#!/bin/bash
if [ $# -ne 1 ]; then
    echo "请传入一个正整数"
    exit 1
fi
result=1
for ((i=1; i<=$1; i++)); do
    result=$((result * i))
done
echo "$1 的阶乘: $result"
```

**④ 猜数字游戏：**
```bash
target=$((RANDOM % 100 + 1))   # 1~100 随机数
# while循环判断 guess 与 target 大小
```

**⑤ 菜单 case：**
```bash
echo "请选择1-3的数字:"
read choice
case $choice in
    1) pwd; date ;;
    2) cd $HOME ;;
    3) ls -a; pwd ;;
    *) echo "输入错误" ;;
esac
```

---

## 十一、进程管理

| 考点 | 内容 |
|------|------|
| 进程 ID | **PID**（Process ID），每个进程唯一 |
| 查看进程 | `top` — 实时动态显示进程信息 |
| 结束进程 | `kill PID` — 按 PID 终止；`killall` — 终止某用户的**所有进程** |
| 终止当前命令 | `Ctrl-C` |
| 进程类型 | 交互进程、批处理进程、守护进程（**就绪进程**不是 Linux 进程类型） |
| 程序 vs 进程 vs 线程 | 程序是静态代码文件；进程是程序的一次执行，有独立内存空间；线程是进程内的执行流，**共享进程资源** |

---

## 十二、正则与文本处理

| 命令 | 功能 | 特点 |
|------|------|------|
| `grep` | 正则匹配查找 | **只读查询**，不修改文件 |
| `sed` | 流式编辑器 | 可**直接修改**文件内容，常用于批量替换 |

---

## 十三、文件传输

| 考点 | 内容 |
|------|------|
| FTP 端口 | **20**（数据）和 **21**（控制） |
| 远程复制 | `scp` 基于 SSH 安全传输 |

---

## 十四、inode（索引节点）

| 考点 | 内容 |
|------|------|
| 定义 | 文件系统中描述文件的元数据结构，包含大小、权限、数据块指针等 |
| 与文件关系 | inode 和文件**不是一一对应**（硬链接多个文件共用一个 inode） |
| 不包含 | 文件名（文件名存在目录的目录项中） |

---

## 十五、其他常用命令速查

| 场景 | 命令 |
|------|------|
| 联机帮助 | `man 命令` |
| 查看日历 | `cal`；`cal 6 2026`；`cal -3`（三个月）；`cal -y`（全年） |
| 查看当前目录 | `pwd` |
| 进程名称结束进程 | `killall 进程名` |

---

## 十六、易错 / 高频混淆点

| 易混淆 | 辨析 |
|--------|------|
| `chmod` vs `chown` | chmod 改权限，chown 改所有者 |
| `userdel` vs `userdel -r` | 加 `-r` 才删家目录 |
| `userdel -r` vs `usermod` | usermod **不能删除账户** |
| `>` vs `>>` | `>` 覆盖，`>>` 追加 |
| 单引号 vs 双引号 | 单引号不解析变量，双引号解析 |
| 软链接 vs 硬链接 | 软链接跨文件系统，删原文件失效；硬链接反之 |
| `cat` vs `less` vs `tail` | cat 一次输出、less 可上下翻页、tail 默认最后10行 |
| `:` vs `.` in vi | `:` 进入末行模式，`.` 重复操作 |

---

## 十七、速查总表（考前必看）

| 场景 | 命令 |
|------|------|
| 关机 | `shutdown -h now` |
| 重启 | `reboot` |
| 添加用户 | `useradd 用户名` |
| 设密码 | `passwd 用户名` |
| 删用户+家目录 | `userdel -r 用户名` |
| 返回上级目录 | `cd ..` |
| 回主目录 | `cd` |
| 查看当前路径 | `pwd` |
| 新建文件 | `touch 文件名` |
| 删除文件 | `rm 文件名` |
| 移动/重命名 | `mv 源 目标` |
| 递归创建目录 | `mkdir -p 路径` |
| 查看文件 | `cat` / `less` / `more` |
| 查看末尾N行 | `tail -n N` |
| 实时追踪日志 | `tail -f` |
| 打包压缩 | `tar -zcvf 包名.tar.gz 目录` |
| 解压 | `tar -xzvf 包名.tar.gz` |
| 创建软链接 | `ln -s 源 目标` |
| vim 进入编辑 | `i`（当前）/ `a`（后）/ `o`（下一行） |
| vim 删除整行 | `dd` |
| vim 复制整行 | `yy` |
| vim 粘贴 | `p` |
| vim 撤销 | `u` |
| vim 重复操作 | `.` |
| vim 保存退出 | `:wq` |
| vim 强制不保存退出 | `:q!` |
| vim 显示行号 | `:set nu` |
| 修改权限 | `chmod 755 文件名` |
| 权限放满 | `chmod 777 文件名` |
| 改所有者 | `chown 用户:组 文件名` |
| 查看进程 | `top` |
| 结束进程 | `kill PID` |
| 结束所有同名进程 | `killall 进程名` |
| 终止当前命令 | `Ctrl-C` |
| 查看帮助 | `man 命令` |
| Shell 脚本头 | `#!/bin/bash` |
| 脚本获取参数 | `$1`, `$2`, `$#` |
| 算术运算 | `$((表达式))` |
| 数值比较 | `-eq` `-ne` `-gt` `-lt` |
| 检查文件存在 | `[ -e 文件 ]` |
| 检查普通文件 | `[ -f 文件 ]` |
| 检查字符设备 | `[ -c 文件 ]` |
