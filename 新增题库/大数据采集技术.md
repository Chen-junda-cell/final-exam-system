课后作业1
题量: 28 满分: 100
作答时间:07-14 11:03至08-26 11:03
智能分析
一. 单选题（共20题）
1. (单选题)大数据处理流程中，数据收集之后的第一步操作是(        )
A. 数据分析
B. 数据可视化
C. 数据存储
D. 数据预处理
我的答案:D:数据预处理;
2. (单选题)以下哪种数据类型属于非结构化数据?(        )
A. MySQL数据库中的表格数据
B. JSON文档
C. 音频文件
D. XML文档
我的答案:C:音频文件;
3. (单选题) 数据采集中，"增量采集"的含义是(        )
A. 每次从第一条数据开始收集
B. 只采集之前没有采集过的数据
C. 采集所有类型的数据
D. 只采集结构化数据
我的答案:B:只采集之前没有采集过的数据;
4. (单选题)以下哪个不是数据采集的要求?(        )
A. 全面性
B. 多维性
C. 随机性
D. 及时性
我的答案:C:随机性;
5. (单选题) 数据采集流程主要包括(        )
A. 采集、分析、可视化
B. 采集、清洗、存储
C. 采集、存储、分析
D. 清洗、存储、可视化
我的答案:B:采集、清洗、存储;
6. (单选题)关于爬虫的描述，正确的是(        )
A. 爬虫可以获取浏览器未展示的数据
B. 爬虫模拟浏览器发送请求，获取响应
C. 爬虫只能获取静态网页数据
D. 爬虫不需要遵循任何规则
我的答案:B:爬虫模拟浏览器发送请求，获取响应;
7. (单选题)HTTPS协议默认端口号是(        )
A. 80
B. 443
C. 8080
D. 21
我的答案:B:443;
8. (单选题)爬虫特别关注的请求头字段中，用于伪装浏览器身份的是(        )
A. Host
B. Connection
C. User-Agent
D. Content-Type
我的答案:C:User-Agent;
9. (单选题)HTTP状态码403表示(        )
A. 请求成功
B. 找不到该页面
C. 服务器内部错误
D. 资源不可用，服务器拒绝处理
我的答案:D:资源不可用，服务器拒绝处理;
10. (单选题)关于浏览器的运行过程，下列说法错误的是(        )
A. 浏览器会发送多个请求获取页面资源
B. 浏览器具有渲染能力
C. 爬虫和浏览器获取的页面内容完全相同
D. 浏览器最终展示的结果由多个响应共同渲染
我的答案:C:爬虫和浏览器获取的页面内容完全相同;
11. (单选题)谷歌浏览器中，用于观察首次访问网站时不携带cookie的模式是(        )
A. 普通窗口
B. 无痕窗口
C. 开发者工具
D. 隐私模式
我的答案:B:无痕窗口;
12. (单选题) urllib模块中，用于构建复杂请求对象的类是(        )
A. urlopen
B. Request
C. urlretrieve
D. urlencode
我的答案:B:Request;
13. (单选题) 以下关于urlopen(        )和Request(        )的描述，正确的是(        )
A. urlopen(        )可以设置请求头
B. Request(        )用于执行请求
C. Request(        )创建的对象需要传递给urlopen(        )执行
D. urlopen(        )不能发送POST请求
我的答案:C:Request( )创建的对象需要传递给urlopen( )执行;
14. (单选题) 正则表达式中，表示"前面的字符至少出现一次"的量词是(        )
A. *
B. +
C. ?
D. {1}
我的答案:B:+;
15. (单选题)re模块中，用于查找所有符合条件的匹配项并返回列表的方法是(        )
A. search(        )
B. match(        )
C. findall(        )
D. finditer(        )
我的答案:C:findall( );
16. (单选题)正则表达式模式 r"^\w+@\w+.(com|cn|org)$" 中，捕获组的数量是(        )
A. 1
B. 2
C. 3
D. 4
我的答案:A:1;
17. (单选题)re.match(        )和re.search(        )的主要区别是(        )
A. match(        )返回匹配对象，search(        )返回字符串
B. match(        )从开头匹配，search(        )在任意位置搜索
C. match(        )只能匹配数字，search(        )能匹配所有字符
D. 两者没有区别
我的答案:B:match( )从开头匹配，search( )在任意位置搜索;
18. (单选题)正则表达式中，将贪婪模式切换为懒惰模式的符号是(        )
A. ^
B. $
C. ?
D. |
我的答案:C:?;
19. (单选题) 以下哪个是基于身份识别的反爬手段?(        )
A. 基于请求频率限制
B. 通过User-Agent字段反爬
C. 通过假数据反爬
D. 阻塞任务队列
我的答案:B:通过User-Agent字段反爬;
20. (单选题)关于反爬虫策略，下列说法正确的是(        )
A. 状态码200一定表示返回了真实数据
B. 所有状态码都可信
C. 状态码不可信，应以抓包获取的响应数据为准
D. 状态码404表示反爬成功
我的答案:C:状态码不可信，应以抓包获取的响应数据为准;
二. 多选题（共5题）
21. (多选题) 以下属于数据处理流程环节的有(        )
A. 数据收集
B. 数据可视化
C. 数据安全与隐私保护
D. 数据删除
我的答案:AB:数据收集; 数据可视化;
22. (多选题) 以下数据类型中，属于半结构化数据的有(        )
A. 数据库表格数据
B. 日志文件
C. XML文档
D. JSON文档
我的答案:BCD:日志文件; XML文档; JSON文档;
23. (多选题) 以下属于数据采集范围的有(        )
A. 数据库中的数据
B. 业务系统中的日志数据
C. 互联网的应用数据
D. 纸质文档数据
我的答案:ABCD:数据库中的数据; 业务系统中的日志数据; 互联网的应用数据; 纸质文档数据;
24. (多选题) 以下哪些属于常见的反爬手段?(        )
A. 通过headers字段反爬
B. 基于请求频率反爬
C. 通过验证码反爬
D. 提供更快的服务器响应
我的答案:ABC:通过headers字段反爬; 基于请求频率反爬; 通过验证码反爬;
25. (多选题) 以下关于urllib模块的说法，正确的有(        )
A. urllib是Python标准库，无需额外安装
B. urlopen(        )可以设置超时时间
C. Request(        )类支持设置请求方法(GET/POST等)
D. urlencode(        )用于将字典转换为URL编码字符串
我的答案:ABCD:urllib是Python标准库，无需额外安装; urlopen( )可以设置超时时间; Request( )类支持设置请求方法(GET/POST等); urlencode( )用于将字典转换为URL编码字符串;
三. 简答题（共3题）
26. (简答题)
简述数据采集的流程，并说明网页爬取和日志数据类型的数据采集流程分别是什么？

我的答案：
通用数据采集流程：明确采集目标→确定数据源→选择采集方式→执行采集→数据清洗/预处理→数据存储。网页爬取流程：确定目标网站→分析页面结构→发送HTTP请求→获取响应→解析提取数据(正则/XPath/JSONPath)→清洗→存储，有翻页则循环请求下一页。日志采集流程：确定日志来源(服务器/业务系统)→部署日志采集工具(如Flume)→实时/定时采集→解析格式化→去重清洗→存储到日志系统或大数据平台。

教师批语：
你本次未作答任何内容，没有覆盖题目要求的任何得分点，包括通用数据采集流程、网页爬取的采集流程以及日志数据类型的采集流程均未涉及。
27. (简答题)
请详细说明爬虫是什么？爬虫的运行流程是怎样的？爬虫可以分为哪几类？

我的答案：
①概念：网络爬虫(Web Crawler/Spider)是模拟浏览器向服务器发送HTTP请求、获取响应并自动解析提取数据的程序。②运行流程：发送请求→获取响应→解析提取数据或进一步的URL→保存数据→(翻页/跟进URL)循环。③分类：按功能分为通用爬虫(搜索引擎)和聚焦爬虫(定向主题)；按抓取方式分为通用网络爬虫、聚焦爬虫、增量式爬虫、深层网络爬虫。

教师批语：
你本次未提交任何作答内容，没有覆盖题目要求的爬虫概念、运行流程、分类这三个部分的任何得分点，无法获得相应分数。
28. (简答题)
请阐述HTTP和HTTPS的区别，列举常见的反爬手段并说明对应的解决思路。

我的答案：
①区别：HTTP明文传输，HTTPS=HTTP+SSL/TLS加密传输、更安全；默认端口HTTP为80、HTTPS为443；HTTPS有证书校验，安全性高但握手更复杂、性能略慢。②常见反爬及解决思路：User-Agent等headers反爬→设置完整浏览器请求头伪装；请求频率/IP反爬→加延时(time.sleep)、使用代理IP池、降低频率；验证码反爬→手动识别/打码平台/OCR识别；登录/Cookie反爬→模拟登录获取Cookie并在请求中携带；动态渲染/假数据反爬→抓包分析真实接口或使用Selenium模拟浏览器。

教师批语：
你只答出了HTTP和HTTPS在传输是否加密这一个区别点，没有提及两者的协议本质、默认端口以及安全性和性能的差异，这部分内容仅能拿到部分分数。另外，题目要求列举常见反爬手段及对应解决思路，你完全没有作答这部分内容。综合来看，你对题目的覆盖度很低，仅答对了HTTP与HTTPS区别中的一个小要点。
一. 单选题（共20题）
1. (单选题) 在Python中，以下哪个库是用于发送HTTP请求的标准库?
A. requests
B. urllib
C. lxml
D. re
我的答案:B:urllib;
2. (单选题)下列关于 requests 模块和 urllib 模块的说法，错误的是:
A. requests 是第三方库，需要额外安装。
B. urllib 是Python标准库的一部分。
C. requests 的API设计比urllib更简洁，易于使用。
D. requests 无法处理Cookie，只能由urllib处理。
我的答案:D:requests 无法处理Cookie，只能由urllib处理。;
3. (单选题)在使用 requests.get(url) 获取网页内容后，response.text 和 response.content 的主要区别是:
A. response.text 返回的是字节类型(bytes)，response.content 返回的是字符串类型(str)。
B. response.text 返回的是字符串类型(str)，response.content 返回的是字节类型(bytes)。
C. 两者返回的都是字符串类型，没有区别。
D. 两者返回的都是字节类型，没有区别。
我的答案:B:response.text 返回的是字符串类型(str)，response.content 返回的是字节类型(bytes)。;
4. (单选题)关于爬虫的 robots.txt 协议，以下描述正确的是:
A. 它是一个强制性的防火墙协议，所有爬虫都必须遵守。
B. 它是一个约定俗成的协议，用于告诉爬虫哪些页面可以抓取，哪些不可以。
C. 它存储在网站的根目录下，但爬虫无法读取到它。
D. 它主要用来保护网站的隐私，具有很强的法律约束力。
我的答案:B:它是一个约定俗成的协议，用于告诉爬虫哪些页面可以抓取，哪些不可以。;
5. (单选题)在XPath语法中，以下哪个符号表示选取当前节点的直接子节点?
A. //
B. /
C. .
D. ..
我的答案:B:/;
6. (单选题)在XPath语法中，以下哪个符号用于选取文档中所有符合条件的节点，不论它们在文档中的位置?
A. //
B. /
C. .
D. ..
我的答案:A://;
7. (单选题)在XPath中，如果要从一个<a>标签中提取其 href 属性的值，正确的语法是?
A. //a/text(        )
B. //a/@href
C. //a/@href/text(        )
D. //a[href]
我的答案:B://a/@href;
8. (单选题)在 requests 库中，使用哪个参数可以设置代理服务器?
A. timeout
B. verify
C. proxies
D. cookies
我的答案:C:proxies;
9. (单选题)在 requests 库中，使用哪个参数可以忽略SSL证书验证?
A. timeout
B. verify
C. proxies
D. cookies
我的答案:B:verify;
10. (单选题)以下哪个是 lxml 库中用于将HTML字符串解析为 Element 对象的正确方法?
A. etree.parse(html_str)
B. etree.HTML(html_str)
C. etree.XML(html_str)
D. etree.tostring(html_str)
我的答案:B:etree.HTML(html_str);
11. (单选题) 使用 jsonpath 模块提取数据时，$..key6 代表什么含义?
A. 从根节点开始，逐级查找名为 key6 的直接子节点。
B. 从根节点开始，查找任意位置名为 key6 的子孙节点。
C. 查找根节点本身。
D. 查找名为 key6 的属性。
我的答案:B:从根节点开始，查找任意位置名为 key6 的子孙节点。;
12. (单选题)以下哪个Python内置模块专门用于处理JSON数据?
A. json
B. pickle
C. csv
D. xml
我的答案:A:json;
13. (单选题)在 requests 库中，timeout 参数的作用是:
A. 设置请求的等待时间，如果服务器在该时间内未响应，则抛出异常。
B. 设置请求的缓存时间。
C. 设置请求的Cookie过期时间。
D. 设置请求的超时重试次数。
我的答案:A:设置请求的等待时间，如果服务器在该时间内未响应，则抛出异常。;
14. (单选题)以下哪个是正确的爬虫伪装请求头的写法?
A. headers = {'User-Agent': 'python-requests/2.32.4'}
B. headers = {'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'}
C. headers = {'User-Agent': 'Chrome/115.0.0.0'}
D. headers = {'User-Agent': 'BaiduSpider'}
我的答案:B:headers = {'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'};
15. (单选题)XPath表达式 //div[contains(@class, "item")] 的含义是:
A. 选取所有 class 属性等于 "item" 的 div 元素。
B. 选取所有 class 属性包含 "item" 字符串的 div 元素。
C. 选取所有 <div> 元素中包含 <item> 子元素的元素。
D. 选取所有 <div> 元素的 class 属性值。
我的答案:B:选取所有 class 属性包含 "item" 字符串的 div 元素。;
16. (单选题)在 lxml 中，etree.tostring(element) 函数的作用是:
A. 将一个 Element 对象序列化为HTML或XML的字节字符串。
B. 将一个 Element 对象转换为Python的字典。
C. 从字符串中解析出一个 Element 对象。
D. 提取 Element 对象中的文本内容。
我的答案:A:将一个 Element 对象序列化为HTML或XML的字节字符串。;
17. (单选题) json.loads('{"name": "张三"}') 代码的返回结果是:
A. '{"name": "张三"}'
B. {"name": "张三"} (Python字典)
C. ("name", "张三")
D. 一个 json 对象
我的答案:B:{"name": "张三"} (Python字典);
18. (单选题) 关于XPath中的 text(        ) 函数，以下说法正确的是:
A. 用于获取一个标签的 text 属性值。
B. 用于获取一个标签的开闭标签之间的文本内容。
C. 用于获取一个标签的内部HTML代码。
D. 用于匹配文本内容为指定值的标签。
我的答案:B:用于获取一个标签的开闭标签之间的文本内容。;
19. (单选题)通过 response.cookies 获取到的Cookie对象是什么类型?
A. 字典 (dict)
B. 列表 (list)
C. RequestsCookieJar 对象
D. 字符串 (str)
我的答案:C:RequestsCookieJar 对象;
20. (单选题)爬取数据时，如果网站返回的HTML内容不完整或缺少结束标签，使用lxml.etree.HTML(        ) 解析时，它会:
A. 直接抛出解析异常。
B. 返回空的 Element 对象。
C. 自动补全缺失的标签，生成一个完整的HTML文档树。
D. 丢弃不完整的部分，只解析完整的标签。
我的答案:C:自动补全缺失的标签，生成一个完整的HTML文档树。;
二. 多选题（共5题）
21. (多选题)以下哪些是 requests 模块响应对象(response)的常见属性或方法?
A. response.text
B. response.content
C. response.status_code
D. response.json(        )
我的答案:ABCD:response.text; response.content; response.status_code; response.json( );
22. (多选题)关于 requests 模块中的 cookies 参数，以下说法正确的是:
A. 它接收一个字典形式的Cookie。
B. 它的用法是 requests.get(url, cookies=cookie_dict)。
C. 它和直接在 headers 中设置Cookie字符串的效果是一样的。
D. 通过 cookies 参数设置的Cookie在请求之间会自动管理和更新。
我的答案:ABC:它接收一个字典形式的Cookie。; 它的用法是 requests.get(url, cookies=cookie_dict)。; 它和直接在 headers 中设置Cookie字符串的效果是一样的。;
23. (多选题)XPath中的节点修饰语法包括哪些?
A. 通过索引修饰节点，如 div[3]
B. 通过属性值修饰节点，如 div[@id="main"]
C. 通过子节点的值修饰节点，如 div[span > 100]
D. 通过包含修饰，如 div[contains(@class, "item")]
我的答案:ABCD:通过索引修饰节点，如 div[3]; 通过属性值修饰节点，如 div[@id="main"]; 通过子节点的值修饰节点，如 div[span > 100]; 通过包含修饰，如 div[contains(@class, "item")];
24. (多选题)以下哪些是数据解析的常用技术或库?
A. 正则表达式 (re)
B. XPath (lxml)
C. JSONPath (jsonpath)
D. urllib
我的答案:ABC:正则表达式 (re); XPath (lxml); JSONPath (jsonpath);
25. (多选题)以下哪些行为可以提高网络爬虫的稳健性和合法性?
A. 设置合理的请求头(User-Agent)。
B. 遵守目标网站的 robots.txt 协议。
C. 使用高频率、多线程的请求，以最快速度获取数据。
D. 在请求之间加上适当的延时(如 time.sleep(        ))。
我的答案:ABD:设置合理的请求头(User-Agent)。; 遵守目标网站的 robots.txt 协议。; 在请求之间加上适当的延时(如 time.sleep( ))。;
课堂作业1
题量: 11 满分: 100
作答时间:07-17 14:35至08-26 14:35
智能分析
一. 单选题（共10题）
1. (单选题)Scrapy框架中，启动爬虫的正确命令是(        )
A. scrapy startproject 爬虫名
B. scrapy genspider 爬虫名
C. scrapy crawl 爬虫名
D. scrapy run 爬虫名
我的答案:C:scrapy crawl 爬虫名;
2. (单选题)在Scrapy中，用于存储爬取数据的模型类定义在哪个文件中(        )
A. settings.py
B. pipelines.py
C. items.py
D. middlewares.py
我的答案:C:items.py;
3. (单选题) 下列哪个命令用于创建一个新的Scrapy项目(        )
A. scrapy genspider
B. scrapy crawl
C. scrapy startproject
D. scrapy run
我的答案:C:scrapy startproject;
4. (单选题)Scrapy爬虫中，解析函数必须命名为(        )
A. parse_item
B. parse
C. handle_response
D. process_response
我的答案:B:parse;
5. (单选题)在Scrapy的settings.py中，设置ROBOTSTXT_OBEY = False的作用是(        )
A. 启用robots.txt协议
B. 忽略robots.txt协议限制
C. 设置爬取速度
D. 启用中间件
我的答案:B:忽略robots.txt协议限制;
6. (单选题)response.xpath(        )方法返回的对象类型是(        )
A. list
B. dict
C. SelectorList
D. str
我的答案:C:SelectorList;
7. (单选题)在Scrapy管道中，必须重写的方法是(        )
A. init
B. process_item
C. close_spider
D. open_spider
我的答案:B:process_item;
8. (单选题)使用yield scrapy.Request(url, callback=self.parse)发送请求时，callback参数的作用是(        )
A. 指定请求方法
B. 指定响应数据的解析函数
C. 指定请求头
D. 指定超时时间
我的答案:B:指定响应数据的解析函数;
9. (单选题)在Scrapy中，提取选择器列表中第一个字符串数据的方法是(        )
A. extract(        )
B. get(        )
C. extract_first(        )
D. get_all(        )
我的答案:B:get( );
10. (单选题)下列哪个不是Scrapy框架的核心组件(        )
A. 引擎(Engine)
B. 调度器(Scheduler)
C. 数据库(Database)
D. 下载器(Downloader)
我的答案:C:数据库(Database);
一. 单选题（共20题）
1. (单选题)大数据处理流程中，数据清洗、去重、去噪、填充缺失值等操作属于哪个阶段?(        )
A. 数据收集
B. 数据存储
C. 数据预处理
D. 数据可视化
我的答案:C:数据预处理;
2. (单选题)以下哪种数据类型属于非结构化数据?(        )
A. 关系型数据库中的表格数据
B. JSON文档
C. XML文档
D. 音频/视频信息
我的答案:D:音频/视频信息;
3. (单选题)爬虫的运行流程中，发送请求获取响应后，下一步应进行的操作是(        )。
A. 直接保存数据
B. 从响应中提取数据或进一步的URL
C. 关闭连接
D. 打印日志
我的答案:B:从响应中提取数据或进一步的URL;
4. (单选题)HTTP与HTTPS的主要区别是(        )。
A. HTTP速度更快
B. HTTPS = HTTP + SSL，更安全
C. HTTP默认端口为443
D. HTTPS不支持加密传输
我的答案:B:HTTPS = HTTP + SSL，更安全;
5. (单选题)在爬虫中，用于伪装浏览器身份的最重要的请求头字段是(        )。
A. Cookie
B. Referer
C. User-Agent
D. Host
我的答案:C:User-Agent;
6. (单选题)在Python的urllib模块中，用于将字典转换为URL编码字符串的函数是(        )。
A. urllib.parse.urlencode(        )
B. urllib.parse.quote(        )
C. urllib.parse.unquote(        )
D. urllib.request.urlopen(        )
我的答案:A:urllib.parse.urlencode( );
7. (单选题)在urllib中，如果要发送携带请求头的复杂请求，需要先创建哪个对象?(        )
A. Request对象
B. Response对象
C. Handler对象
D. Opener对象
我的答案:A:Request对象;
8. (单选题)正则表达式中，+号表示的含义是(        )。
A. 前面的字符出现0次或1次
B. 前面的字符出现0次或多次
C. 前面的字符出现1次或多次
D. 前面的字符出现恰好n次
我的答案:C:前面的字符出现1次或多次;
9. (单选题)在Python的re模块中，re.search(        )与re.match(        )的区别是(        )。
A. search(        )从字符串开头匹配，match(        )从任意位置匹配
B. match(        )从字符串开头匹配，search(        )从任意位置匹配
C. 两者功能完全相同
D. search(        )只返回第一个匹配，match(        )返回所有匹配
我的答案:B:match( )从字符串开头匹配，search( )从任意位置匹配;
10. (单选题)在requests模块中，response.text和response.content的区别是(        )。
A. text返回bytes类型，content返回str类型
B. text返回str类型，content返回bytes类型
C. 两者返回类型相同
D. text是编码后的数据，content是未编码的数据
我的答案:B:text返回str类型，content返回bytes类型;
11. (单选题)在requests模块中，发送带参数的GET请求应使用哪个参数?(        )
A. data
B. json
C. params
D. body
我的答案:C:params;
12. (单选题)在requests模块中，设置代理IP应使用哪个参数?(        )
A. proxy
B. proxies
C. proxy_server
D. forward
我的答案:B:proxies;
13. (单选题)以下关于XPath语法的描述，错误的是(        )。
A. /表示从根节点选取
B. //表示从匹配选择的当前节点选择文档中的节点，不考虑位置
C. @用于选取属性
D. text(        )用于选取属性的值
我的答案:D:text( )用于选取属性的值;
14. (单选题)在XPath中，选取当前节点的父节点应使用(        )。
A. ..
B. .
C. parent
D. ^
我的答案:A:..;
15. (单选题)在lxml模块中，etree.HTML(html_str)方法的作用是(        )。
A. 将HTML字符串转换为bytes类型
B. 将HTML字符串转换为Element对象
C. 将Element对象转换为HTML字符串
D. 对HTML字符串进行加密
我的答案:B:将HTML字符串转换为Element对象;
16. (单选题)在Scrapy框架中，用于存放爬取数据模型的文件是(        )。
A. settings.py
B. pipelines.py
C. items.py
D. middlewares.py
我的答案:C:items.py;
17. (单选题)在Scrapy中，启动一个名为itcast的爬虫应使用的命令是(        )。
A. scrapy run itcast
B. scrapy start itcast
C. scrapy crawl itcast
D. scrapy spider itcast
我的答案:C:scrapy crawl itcast;
18. (单选题)在Scrapy的settings.py中，设置ROBOTSTXT_OBEY = False的作用是(        )。
A. 启用robots.txt协议
B. 禁用robots.txt协议，允许爬取被禁止的页面
C. 启用日志记录
D. 设置下载延迟
我的答案:B:禁用robots.txt协议，允许爬取被禁止的页面;
19. (单选题)在Selenium中，切换标签页(窗口)应使用的方法是(        )。
A. driver.switch_to.frame(        )
B. driver.switch_to.window(        )
C. driver.switch_to.alert(        )
D. driver.switch_to.default_content(        )
我的答案:B:driver.switch_to.window( );
20. (单选题)在BeautifulSoup中，根据CSS选择器查找元素应使用的方法是(        )。
A. find(        )
B. find_all(        )
C. select(        )
D. search(        )
我的答案:C:select( );
二. 简答题（共2题）
21. (简答题) 请简述Scrapy框架的工作流程(从起始URL到数据保存的完整过程)。
我的答案：
Scrapy工作流程：①Spider把起始URL交给Engine；②Engine把URL放入Scheduler排队；③Scheduler把下一请求返回Engine；④Engine经下载器中间件把请求交给Downloader下载；⑤Downloader把响应返回Engine再交给Spider；⑥Spider用parse()等回调解析响应，提取Item或新的URL；⑦Item交给Item Pipeline清洗、去重、持久化；⑧新URL回到Scheduler，重复②-⑦直到无新请求。
教师批语：
学生未作答。
22. (简答题) 请简述Scrapy框架中各个核心组件(引擎、调度器、下载器、爬虫、管道)的作用及其在整个工作流程中的职责。
我的答案：
①引擎(Engine)：核心，协调各组件间数据流、触发事务；②调度器(Scheduler)：维护待爬取请求队列，负责排队与去重；③下载器(Downloader)：发送请求、下载网页响应并交给引擎；④爬虫(Spider)：解析响应，提取数据(Item)和新的URL请求；⑤管道(Item Pipeline)：处理Spider提取的数据，负责清洗、校验、去重、持久化存储。
教师批语：
学生未作答。
一. 简答题（共1题）
1. (简答题)
案例：完成一个基于 Scrapy 的安居客自己家乡的二手房爬虫，要求爬取房源的标题、房源名、地址、房源描述，并将数据保存到anjuke.json文件中，并实现翻页功能。（比较重要，直接将答案写到空白处）

任务要求：

①    Items 模型定义

②    爬虫核心逻辑（数据提取 + 翻页）

③    数据管道处理

④Settings 配置（反爬 + 管道启用）

